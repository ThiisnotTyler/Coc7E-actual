# Secrets — never commit
config/api-key.json
config/service-account.json

# Runtime state — never commit (a path bug once nested saves/saves/... into git)
saves/
logs/
chronicle/
investigators.json

# Python
__pycache__/
*.pyc
